specVersion: "v1"
serviceId: "typing-speed-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "typing-speed-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
